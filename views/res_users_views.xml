<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Heredar vista de formulario de usuarios -->
    <record id="view_users_form_cartera" model="ir.ui.view">
        <field name="name">res.users.form.cartera</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            
            <xpath expr="//page[@name='access_rights']//group" position="after">
                <group string="Cartera de Creditos">
                    <field name="cartera_role" widget="radio"/>
                </group>
            </xpath>
            
        </field>
    </record>
</odoo>
